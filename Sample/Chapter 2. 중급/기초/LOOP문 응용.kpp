함수 i, z, k, a, b, q

제목("Loop문 응용")

출력("띄우기" & "숫자 : ")

a~입력("문자")

출력("띄우기" & "숫자 : ")

b~입력("문자")

출력("띄우기" & "모양 : ")

q~입력("문자")


루프 (z) < (a)

z = z + "1"
k = k & q

끝


루프 (i) < (b)

i = i + "1"
출력("띄우기" & k)

끝
