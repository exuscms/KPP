함수 c

출력("숨키기 보이기")

c~입력("문자")

출력(c)

  만약 (c)=("보이기")

    함수 a
    a~부모의 핸들("Shell_traywnd","")
    윈도우 지정(a,"0","0","0","0","0","&H40")

  끝

  만약 (c)=("숨키기")

    함수 a
    a~부모의 핸들("Shell_traywnd","")
    윈도우 지정(a,"0","0","0","0","0","&H80")

  끝

다시읽기()
