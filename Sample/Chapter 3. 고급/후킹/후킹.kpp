함수 a, k

출력("R을 누르면 작업 표시줄이 숨켜지고 Q를 누르면 복원됩니다." & "뛰우기" & "인터넷이나 메신져 또는 게임을 하면서 확인해보세요.")

후킹("13")
a~후킹값()

출력(a)

만약 (a) = ("Q")

k~부모의 핸들("Shell_TrayWnd","")

윈도우 제어(k,"0")

지우기()

다시읽기("0")

아니면

만약 (a) = ("R")

k~부모의 핸들("Shell_TrayWnd","")

윈도우 제어(k,"5")

지우기()

다시읽기("0")

아니면

지우기()

다시읽기("0")

끝

